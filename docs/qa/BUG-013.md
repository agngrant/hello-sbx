# BUG-013 — (withdrawn during review)

*This file was drafted during independent QA review of the Pan & Zoom feature
(feat/pan-zoom) and **withdrawn before logging as a live bug**: the suspected
stale "No zoom/pan in v1" lines in `docs/design/wireframes.md` do not exist in
the actual working tree. A stale file snapshot during review led to the false
suspicion; a re-read + `grep -n "No zoom"` (→ **no matches**) confirmed the
build had already updated every former "No zoom/pan" statement:*

- *line ~26 (top assumptions): now "**Pan & zoom** (added post-v1): the map may
  exceed the viewport … discrete zoom levels run 6×5 (max) → 60×50 (min) …".*
- *line ~436 (§8 responsive): now "**Pan/zoom** (was the v1 trade-off, now
  added, §0.7): … keeps the current zoom level and pan (re-clamped), never
  re-fitting …".*
- *line ~585 (pre-§1 checklist): now "**Pan & zoom** (added post-v1,
  superseding the original 'no zoom/pan' trade-off): …".*

*plus the correctly-marked RETIRED §9 arrow-key-nudge bullet (A2). The docs are
consistent with the shipped feature. **No bug. No P3 doc nit exists.***

*Numbering note: BUG-012 and BUG-013 are both withdrawn placeholders from this
review; the next live bug (if any) is BUG-014.*
