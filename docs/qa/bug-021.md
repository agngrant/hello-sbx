# BUG-021 — `boss_footprint` import broken: models rename not propagated (boss gate)

- **Status:** OPEN — found on QA gate re-run at `2c89ede` (feat/boss-entity)
- **Severity:** Critical — full pytest suite interrupted at collection; 0 tests executed
- **Origin:** `27ee6d6` renamed `boss_footprint` → `boss_footprint_cells` in `app/models.py` (strict `BOSS_FOOTPRINTS` = (2,4,6,8,10,12), no 1×1 fallback) without updating importers; `2c89ede` (BUG-020 fixture alignment) also left the old import in `tests/test_boss.py`.
- **Affected (all `ImportError: cannot import name 'boss_footprint' from 'app.models'`):**
  - `app/session.py` (import, line 46) → cascades into `tests/test_session.py`, `tests/test_ws.py`, `tests/test_api.py`, `tests/test_saves.py`, `tests/test_door_session.py`
  - `tests/test_boss.py` (direct import + call in `test_invalid_boss_size_is_rejected`)
- **Gate result (this run):** pytest exit=2 — 6 collection errors, 0 tests ran; `node --check app/static/app.js` exit=0 (PASS).
- **Log:** `.logs/qa.log` (counts + error lines only, no stack traces).
- **Suggested fix:** point `app/session.py` and `tests/test_boss.py` at `boss_footprint_cells` (or re-add a `boss_footprint = boss_footprint_cells` alias in `app/models.py` if the old name is the intended public API); then re-run the gate.
