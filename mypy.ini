[mypy]
# Mypy gate for the backend package only.
#
# Scope: run with `.venv/bin/python -m mypy app/` — the `app/` argument
# (and `files = app` below) restricts checking to the backend package;
# tests/ and scripts/ are intentionally out of scope.
#
# This config is intentionally minimal (no strict flags) so the baseline
# error count stays stable; tightening flags is a later stage.

files = app
python_version = 3.14
warn_unused_configs = true

# ---------------------------------------------------------------------------
# Frozen baseline (mypy 2.3.1, Python 3.14.4, run `.venv/bin/python -m mypy app/`)
# Stage 1 baseline freeze — DO NOT let the count grow; only shrink it.
#
#   25 errors in 6 files (checked 14 source files)
#
#   app/detection.py  2  (lines 182, 183)
#   app/imaging.py    2  (lines 121, 123)
#   app/saves.py      7  (lines 89, 91, 93, 248 x2, 250 x2)
#   app/server.py     5  (lines 137, 363, 364, 366, 368)
#   app/session.py    8  (lines 426, 640, 691, 696 x2, 959, 1040, 1079)
#   app/ws.py         1  (line 212)
#
# DELTA vs the pre-edit expectation of "28 errors in 7 files": the re-verified
# concurrent edit to app/models.py added a behavior-preserving mypy narrowing
# (`or self.safe is None`, logically redundant since is_safe_door already
# implies `self.safe is not None`), which cleared all 3 of models.py's errors.
# 28 -> 25, 7 -> 6 files. models.py is now clean.
# ---------------------------------------------------------------------------
